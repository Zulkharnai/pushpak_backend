/**
 * Stringifies an object to base64url-encoded JSON.
 * @param obj - Object to encode.
 * @returns The encoded object.
 */
export declare function encodeObject(obj: any): string;
/**
 * Parses an object from base64url-encoded JSON.
 *
 * @remarks
 * This function will throw an `InvalidJsonError` if parsing fails.
 *
 * @param str - The encoded object.
 * @returns - The decoded object.
 */
export declare function decodeObject(str: string): any;
