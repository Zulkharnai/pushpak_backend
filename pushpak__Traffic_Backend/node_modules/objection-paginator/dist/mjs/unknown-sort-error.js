import { ObjectionPaginatorError } from "./objection-paginator-error.js";
export class UnknownSortError extends ObjectionPaginatorError {
    static getDefaultMessage(info) {
        let msg = "Unknown sort";
        if (info && "sort" in info)
            msg += `: '${info.sort}'`;
        return msg;
    }
}
//# sourceMappingURL=unknown-sort-error.js.map